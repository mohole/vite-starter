{
  "name": "mohole-starter",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d ./dist"
  },
  "devDependencies": {
    "gh-pages": "^4.0.0",
    "sass": "^1.52.2",
    "vite": "^2.9.9"
  },
  "dependencies": {
    "bootstrap": "^5.2.0-beta1"
  }
}
